# Local development environment variables. Copy this file to .env and fill
# in real values - NEVER commit .env (it's already in .gitignore).
#
# On Render these variables are NOT read from this file, they are set
# directly in the Render dashboard under "Environment".

SECRET_KEY=dein-lokaler-geheimer-schluessel
DEBUG=True
ALLOWED_HOSTS=127.0.0.1,localhost
CORS_ALLOWED_ORIGINS=http://localhost:4200,http://127.0.0.1:4200

# Optional locally, required on Render:
# DATABASE_URL=postgresql://user:password@host/dbname?sslmode=require
